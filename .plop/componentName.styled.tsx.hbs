import styled from "styled-components";
import {{ properCase name }}Props from "../{{ properCase name }}.props"

const Styled{{ properCase name }} = styled.div<{{ properCase name }}Props>`
  color: ${({ theme }) => theme.colors.primaryColor};
`;

export default Styled{{ properCase name }};
