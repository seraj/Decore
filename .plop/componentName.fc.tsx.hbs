import * as React from "react";
import Styled{{ properCase name }} from "./styled/Styled{{ properCase name }}";
import {{ properCase name}}Props from "./{{ properCase name }}.props";

const {{ properCase name }}: React.FC<{{ properCase name }}Props> = ({ children, ...props }) => (
  <Styled{{ properCase name}} {...props}>{children}</Styled{{ properCase name }}>
);

export default {{ properCase name }};
